Priority note (2026-08-15): deprioritized behind the ingester rate-limit visibility issue (getManagerPicksForGW() treats a 429 identically to genuine no-data, and skips its own self-throttle on failure). That one affects the league we actually run today regardless of size; this issue's pagination gap and the 100-entry cap only bite once a league is bigger than one FPL results page, which isn't our current league.
