Priority note (2026-08-15): deprioritized behind ingester reliability work for our existing single league -- specifically the rate-limit visibility issue (getManagerPicksForGW() treats a 429 identically to genuine no-data, and skips its own self-throttle on failure). That affects the league we actually run today regardless of size; this issue and #49's scoping fix only matter once a second/larger league exists.
